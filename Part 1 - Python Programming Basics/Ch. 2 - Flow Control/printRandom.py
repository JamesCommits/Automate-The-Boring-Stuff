# Importing the random function which will give us access
# to the random.randint() function to use
import random
for i in range(5):
    # Calling the random module and using the randomint function
    print(random.randint(1, 10))